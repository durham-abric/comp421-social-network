IF  EXISTS( SELECT *
                  FROM Post AS p
                  JOIN PageOwner AS po
                  ON p.page = po.pageID
                  WHERE p.pID = post
                  AND po.privacy = 'public')
          THEN (SET visible = 1)